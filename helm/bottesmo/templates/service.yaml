apiVersion: v1
kind: Service
metadata:
  name: {{ include "bottesmo.fullname" . }}
  labels:
    {{- include "bottesmo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: {{ .Values.service.sessionAffinity }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bottesmo.selectorLabels" . | nindent 4 }}
